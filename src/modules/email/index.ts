/**
 * Email module barrel — no delivery actions registered today.
 * Imported by `src/modules/index.ts` for parity; the schema lives in
 * `src/db/migrations/module-email.ts`, the channel adapter in
 * `src/channels/email.ts`.
 */
export {};
